<UserControl x:Class="MPTvServies2MKV.Tips"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <Style x:Key="hintStyle" TargetType="Label">
      <Setter Property="Background" Value="LemonChiffon" />
      <Setter Property="FontStyle" Value="Italic" />
      <Setter Property="Padding" Value="8,4" />
      <Setter Property="Margin" Value="4" />
      <Setter Property="BorderThickness" Value="2"/>
      <Setter Property="BorderBrush" Value="Moccasin"/>
    </Style>
  </UserControl.Resources>
  <StackPanel>
    <Label Style="{StaticResource hintStyle}">You can DoubleClick on LogItems to show the files in Explorer.</Label>
  </StackPanel>
</UserControl>
